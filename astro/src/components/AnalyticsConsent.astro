---
/**
 * Public-site analytics loader.
 *
 * The browser asset is intentionally kept in /public so the static homepage
 * can load the exact same consent flow. It does not contact Google until a
 * visitor explicitly grants analytics consent.
 */
---
<script is:inline src="/scripts/brisbane-analytics.js" defer></script>
