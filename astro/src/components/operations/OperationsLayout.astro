---
export type OperationsSection = "overview" | "workspace" | "leads" | "inbox" | "analytics";

interface Props {
  active: OperationsSection;
  title: string;
  description: string;
}

const { active, title, description } = Astro.props;

const navigation: Array<{ key: OperationsSection; href: string; label: string; icon: string }> = [
  { key: "overview", href: "/operations/", label: "Overview", icon: "⌂" },
  { key: "leads", href: "/operations/leads/", label: "Leads", icon: "◎" },
  { key: "workspace", href: "/operations/workspace/", label: "Calls, quotes & jobs", icon: "◇" },
  { key: "inbox", href: "/operations/inbox/", label: "Mailbox", icon: "✉" },
  { key: "analytics", href: "/operations/analytics/", label: "Google analytics", icon: "↗" },
];

const coreNavigation = navigation.filter((item) => item.key !== "analytics");
const marketingNavigation = navigation.filter((item) => item.key === "analytics");
---

<!doctype html>
<html lang="en-AU">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex,nofollow" />
    <meta name="color-scheme" content="dark light" />
    <title>{title} | Brisbane TVs Operations</title>
    <meta name="description" content={description} />
    <script is:inline>
      (() => {
        let savedTheme = "";
        try {
          savedTheme = localStorage.getItem("brisbane-operations-theme") || "";
        } catch {}
        document.documentElement.dataset.theme = savedTheme === "light" ? "light" : "dark";
      })();
    </script>
  </head>
  <body>
    <div class="operations-shell">
      <aside class="operations-sidebar" aria-label="Operations navigation">
        <div class="operations-brand-wrap">
          <a class="operations-brand" href="/operations/" aria-label="Brisbane TVs operations">
            <span class="operations-brand-mark" aria-hidden="true"><i></i><i></i></span>
            <span><strong>BRISBANE</strong><small>TVS · OPERATIONS</small></span>
          </a>
        </div>

        <nav class="operations-nav" aria-label="Staff workspace">
          {coreNavigation.map((item) => (
            <a
              class:list={["operations-nav-link", { "is-active": active === item.key }]}
              href={item.href}
              aria-current={active === item.key ? "page" : undefined}
            >
              <span class="operations-nav-icon" aria-hidden="true">{item.icon}</span>
              <span class="operations-nav-label">{item.label}</span>
            </a>
          ))}
          <p class="operations-nav-group-label">Marketing</p>
          {marketingNavigation.map((item) => (
            <a
              class:list={["operations-nav-link", { "is-active": active === item.key }]}
              href={item.href}
              aria-current={active === item.key ? "page" : undefined}
            >
              <span class="operations-nav-icon" aria-hidden="true">{item.icon}</span>
              <span class="operations-nav-label">{item.label}</span>
            </a>
          ))}
        </nav>

        <div class="operations-sidebar-footer">
          <button class="operations-theme-toggle" type="button" data-theme-toggle>
            <span class="operations-theme-icon" data-theme-icon aria-hidden="true">☀</span>
            <span data-theme-label>Light mode</span>
          </button>
          <div class="operations-sidebar-note">
            <span class="operations-secure-dot" aria-hidden="true"></span>
            <p><strong>Secure staff area</strong><br />Protected by Cloudflare Access</p>
          </div>
        </div>
      </aside>

      <div class="operations-content">
        <header class="operations-mobile-header">
          <a class="operations-brand" href="/operations/" aria-label="Brisbane TVs operations">
            <span class="operations-brand-mark" aria-hidden="true"><i></i><i></i></span>
            <span><strong>BRISBANE</strong><small>TVS · OPERATIONS</small></span>
          </a>
          <div class="operations-mobile-actions">
            <span class="operations-mobile-secure"><span class="operations-secure-dot" aria-hidden="true"></span> Secure</span>
            <button class="operations-theme-toggle is-compact" type="button" data-theme-toggle>
              <span class="operations-theme-icon" data-theme-icon aria-hidden="true">☀</span>
              <span data-theme-label>Light mode</span>
            </button>
          </div>
        </header>

        <nav class="operations-mobile-nav" aria-label="Staff workspace">
          {navigation.map((item) => (
            <a
              class:list={["operations-mobile-nav-link", { "is-active": active === item.key }]}
              href={item.href}
              aria-current={active === item.key ? "page" : undefined}
            >{item.label}</a>
          ))}
        </nav>

        <main class="operations-main">
          <slot />
        </main>
      </div>
    </div>
    <script is:inline>
      (() => {
        const root = document.documentElement;
        const toggles = Array.from(document.querySelectorAll("[data-theme-toggle]"));

        const renderTheme = () => {
          const dark = root.dataset.theme !== "light";
          toggles.forEach((toggle) => {
            const label = toggle.querySelector("[data-theme-label]");
            const icon = toggle.querySelector("[data-theme-icon]");
            const nextTheme = dark ? "light" : "dark";
            toggle.setAttribute("aria-label", `Switch to ${nextTheme} mode`);
            toggle.setAttribute("aria-pressed", String(dark));
            if (label) label.textContent = `${nextTheme[0].toUpperCase()}${nextTheme.slice(1)} mode`;
            if (icon) icon.textContent = dark ? "☀" : "☾";
          });
        };

        toggles.forEach((toggle) => {
          toggle.addEventListener("click", () => {
            root.dataset.theme = root.dataset.theme === "light" ? "dark" : "light";
            try {
              localStorage.setItem("brisbane-operations-theme", root.dataset.theme);
            } catch {}
            renderTheme();
          });
        });

        window.addEventListener("storage", (event) => {
          if (event.key !== "brisbane-operations-theme") return;
          root.dataset.theme = event.newValue === "light" ? "light" : "dark";
          renderTheme();
        });

        renderTheme();
      })();
    </script>
  </body>
</html>

<style is:global>
  :root {
    color: #131619;
    background: #f5f4f0;
    color-scheme: light;
    font-family: "DM Sans", Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  }

  :root[data-theme="dark"] {
    color: #ececec;
    background: #212121;
    color-scheme: dark;
    --operations-canvas: #212121;
    --operations-sidebar: #171717;
    --operations-surface: #2f2f2f;
    --operations-surface-soft: #282828;
    --operations-surface-hover: #3a3a3a;
    --operations-border: #424242;
    --operations-border-soft: #363636;
    --operations-text: #ececec;
    --operations-text-strong: #f7f7f8;
    --operations-muted: #b4b4b4;
    --operations-subtle: #8e8e8e;
    --operations-accent: #10a37f;
    --operations-accent-soft: #163c33;
    --operations-warning: #f0b45a;
    --operations-warning-soft: #44351c;
    --operations-danger: #ff8c85;
    --operations-danger-soft: #4a2725;
    --operations-info-soft: #263238;
  }

  * { box-sizing: border-box; }
  body { min-width: 320px; margin: 0; background: #f5f4f0; }
  button, input, select, textarea { font: inherit; }
  button, a, input, select, textarea { -webkit-tap-highlight-color: transparent; }

  .operations-shell { display: grid; grid-template-columns: 14.875rem minmax(0, 1fr); min-height: 100vh; }
  .operations-sidebar { display: flex; position: sticky; top: 0; flex-direction: column; height: 100vh; padding: 1.55rem 1.05rem 1rem; background: #131619; color: #fff; }
  .operations-brand { display: flex; align-items: center; gap: .7rem; color: #fff; text-decoration: none; }
  .operations-brand-mark { position: relative; display: block; flex: 0 0 auto; width: 1.85rem; height: 1.85rem; border: 1.5px solid currentColor; }
  .operations-brand-mark i { position: absolute; top: .52rem; left: .5rem; width: .72rem; height: 1px; background: currentColor; transform: rotate(45deg); }
  .operations-brand-mark i + i { top: 1.02rem; transform: rotate(-45deg); }
  .operations-brand strong, .operations-brand small { display: block; }
  .operations-brand strong { font-size: .82rem; letter-spacing: .16em; }
  .operations-brand small { margin-top: .12rem; color: #9ea4a8; font-size: .49rem; letter-spacing: .26em; }
  .operations-nav { display: grid; gap: .2rem; margin-top: 2.35rem; }
  .operations-nav-link { display: flex; align-items: center; gap: .68rem; border-radius: .5rem; padding: .66rem .7rem; color: #aeb3b8; text-decoration: none; transition: background .18s ease, color .18s ease; }
  .operations-nav-link:hover { background: #202428; color: #fff; }
  .operations-nav-link.is-active { background: #fff; color: #131619; box-shadow: none; }
  .operations-nav-icon { display: grid; width: 1.12rem; place-items: center; font-size: 1rem; }
  .operations-nav-label { font-size: .78rem; font-weight: 650; }
  .operations-nav-group-label { margin: 1.45rem .7rem .4rem; color: #636a70; font-size: .55rem; font-weight: 750; letter-spacing: .18em; text-transform: uppercase; }
  .operations-sidebar-footer { display: grid; gap: .8rem; margin-top: auto; }
  .operations-theme-toggle { display: flex; width: 100%; min-height: 2.45rem; align-items: center; gap: .65rem; border: 1px solid #34383b; border-radius: .55rem; padding: .55rem .7rem; color: #d5d7d8; background: #202224; cursor: pointer; font-size: .76rem; font-weight: 720; text-align: left; transition: border-color .18s ease, background .18s ease, color .18s ease; }
  .operations-theme-toggle:hover { border-color: #565b5f; color: #fff; background: #292c2f; }
  .operations-theme-toggle:focus-visible { outline: 3px solid rgba(16, 163, 127, .35); outline-offset: 2px; }
  .operations-theme-icon { display: grid; width: 1.2rem; height: 1.2rem; place-items: center; font-size: .95rem; line-height: 1; }
  .operations-sidebar-note { display: flex; align-items: flex-start; gap: .65rem; border-top: 1px solid #292d30; padding: 1rem .35rem 0; color: #8c9499; }
  .operations-sidebar-note p { margin: 0; font-size: .76rem; line-height: 1.48; }
  .operations-sidebar-note strong { color: #fff; font-weight: 750; }
  .operations-secure-dot { display: inline-block; flex: 0 0 auto; width: .52rem; height: .52rem; margin-top: .31rem; border-radius: 999px; background: #29875d; box-shadow: 0 0 0 4px rgba(41, 135, 93, .12); }

  .operations-content { min-width: 0; }
  .operations-mobile-header, .operations-mobile-nav { display: none; }
  .operations-mobile-actions { display: flex; align-items: center; gap: .65rem; }
  .operations-main { width: min(1400px, calc(100% - 4.5rem)); margin: 0 auto; padding: 2.15rem 0 4.5rem; }
  .operations-page-header { display: flex; align-items: flex-end; justify-content: space-between; gap: 2rem; margin-bottom: 2rem; }
  .operations-eyebrow { margin: 0 0 .48rem; color: #8a8a82; font-size: .58rem; font-weight: 750; letter-spacing: .16em; text-transform: uppercase; }
  .operations-title, .operations-section-title { margin: 0; color: #131619; font-family: Georgia, "Times New Roman", serif; font-weight: 400; letter-spacing: -.025em; }
  .operations-title { font-size: clamp(2rem, 4vw, 3rem); line-height: 1; }
  .operations-section-title { font-size: clamp(1.25rem, 2.1vw, 1.55rem); line-height: 1.08; }
  .operations-copy { max-width: 50rem; margin: 1rem 0 0; color: #5c6a75; font-size: .97rem; line-height: 1.65; }
  .operations-status { max-width: 21rem; margin: 0; border: 1px solid #ddd9cf; border-radius: .7rem; padding: .85rem .95rem; color: #5e6d78; background: #fffdf8; font-size: .84rem; line-height: 1.45; }
  .operations-status.is-ready { border-color: #b7d8c7; color: #176444; background: #f0faf4; }
  .operations-status.is-warning { border-color: #eadbbd; color: #765924; background: #fffbef; }

  .operations-grid { display: grid; gap: 1rem; }
  .operations-metrics { grid-template-columns: repeat(4, minmax(0, 1fr)); }
  .operations-panel, .operations-metric { border: 1px solid #e7e5e0; border-radius: .8rem; background: #fff; box-shadow: 0 1px 0 rgba(0, 0, 0, .01); }
  .operations-panel { padding: 1.45rem; }
  .operations-metric { display: flex; min-height: 9.5rem; flex-direction: column; padding: 1.3rem; }
  .operations-metric-label { margin: 0; color: #61707d; font-size: .84rem; font-weight: 700; }
  .operations-metric-value { display: block; margin: .7rem 0 .4rem; color: #172736; font-family: Georgia, "Times New Roman", serif; font-size: clamp(2.05rem, 3.7vw, 2.9rem); line-height: 1; }
  .operations-metric-copy { margin-top: auto; color: #7a858e; font-size: .76rem; line-height: 1.4; }
  .operations-metric.is-dark { border-color: #131619; color: #fff; background: #131619; }
  .operations-metric.is-dark .operations-metric-label, .operations-metric.is-dark .operations-metric-copy { color: #c5d0d7; }
  .operations-metric.is-dark .operations-metric-value { color: #fff; }
  .operations-panel-heading { display: flex; align-items: flex-start; justify-content: space-between; gap: 1rem; }
  .operations-panel-copy { margin: .7rem 0 0; color: #697680; font-size: .88rem; line-height: 1.5; }
  .operations-link { display: inline-flex; align-items: center; justify-content: center; min-height: 2.45rem; border: 1px solid #131619; border-radius: .55rem; padding: .55rem .8rem; color: #fff; background: #131619; font-size: .84rem; font-weight: 780; text-decoration: none; }
  .operations-link:hover { background: #2a2f33; }
  .operations-link.is-quiet { border-color: #d9d6cf; color: #344352; background: #fffdf9; }
  .operations-link.is-quiet:hover { border-color: #82909a; background: #f4f2ed; }
  .operations-button { min-height: 2.45rem; border: 1px solid #d9d6cf; border-radius: .55rem; padding: .55rem .8rem; color: #344352; background: #fffdf9; cursor: pointer; font-size: .84rem; font-weight: 780; }
  .operations-button:hover:not(:disabled) { border-color: #82909a; background: #f4f2ed; }
  .operations-button:disabled { cursor: not-allowed; opacity: .48; }
  .operations-primary-button { border-color: #172e40; color: #fff; background: #172e40; }
  .operations-primary-button:hover:not(:disabled) { background: #25445b; }
  .operations-button:focus-visible, .operations-link:focus-visible, .operations-nav-link:focus-visible, .operations-mobile-nav-link:focus-visible, .operations-input:focus-visible { outline: 3px solid rgba(67, 112, 144, .32); outline-offset: 2px; }

  .operations-action-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); margin-top: 1rem; }
  .operations-action-card { display: flex; min-height: 15rem; flex-direction: column; }
  .operations-action-card .operations-link { align-self: flex-start; margin-top: auto; }
  .operations-action-card.is-leads { background: #fffdf9; }
  .operations-action-card.is-workspace { border-color: #ccd9df; background: #f1f6f8; }
  .operations-action-card.is-inbox { border-color: #eadfc9; background: #fbf8f1; }
  .operations-action-card.is-analytics { border-color: #d7e4df; background: #eef4f2; }
  .operations-pill { display: inline-flex; align-items: center; min-height: 1.9rem; border-radius: 999px; padding: .3rem .58rem; color: #176444; background: #dceee5; font-size: .72rem; font-weight: 800; white-space: nowrap; }
  .operations-pill.is-muted { color: #5f6d77; background: #ece9e1; }
  .operations-pill.is-warning { color: #765924; background: #f6ebd2; }

  .operations-breakdown-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); margin-top: 1rem; }
  .operations-analytics-metrics { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .operations-analytics-metrics .operations-metric { min-height: 8.75rem; }
  .operations-analytics-metrics .operations-metric-value { font-size: clamp(1.95rem, 3vw, 2.5rem); }
  .operations-list { display: grid; gap: .65rem; margin: 1.2rem 0 0; padding: 0; list-style: none; }
  .operations-list li { display: flex; align-items: flex-start; justify-content: space-between; gap: 1rem; border-top: 1px solid rgba(127, 137, 137, .18); padding-top: .65rem; color: #53636d; font-size: .84rem; line-height: 1.4; }
  .operations-list li strong { color: #1b2c3a; font-size: .78rem; text-align: right; }
  .operations-list .operations-list-loading { justify-content: flex-start; color: #7a858f; }
  .operations-privacy-note { display: flex; align-items: flex-start; gap: .75rem; margin-top: 1rem; border-radius: .8rem; padding: 1rem 1.1rem; background: #e9eff0; color: #50616c; }
  .operations-privacy-note > span { color: #277a59; font-size: 1.2rem; line-height: 1.2; }
  .operations-privacy-note p { margin: 0; font-size: .87rem; line-height: 1.55; }
  .operations-privacy-note a { color: inherit; font-weight: 780; }

  .operations-toolbar { display: flex; align-items: flex-end; justify-content: space-between; gap: 1rem; margin-bottom: 1.1rem; }
  .operations-toolbar-copy { margin: 0; color: #75818a; font-size: .82rem; line-height: 1.45; }
  .operations-filters { display: grid; grid-template-columns: minmax(9rem, .42fr) minmax(13rem, 1fr) auto; gap: .75rem; margin: 1.45rem 0 1rem; }
  .operations-field { display: grid; gap: .35rem; color: #60707d; font-size: .75rem; font-weight: 760; letter-spacing: .02em; }
  .operations-input { width: 100%; min-width: 0; border: 1px solid #d9d6cf; border-radius: .55rem; padding: .64rem .68rem; color: #1e2f3e; background: #fff; font-size: .9rem; line-height: 1.35; }
  textarea.operations-input { min-height: 8rem; resize: vertical; }
  .operations-filters .operations-button { align-self: end; }
  .operations-table-wrap { overflow-x: auto; border: 1px solid #e7e3dc; border-radius: .65rem; }
  .operations-table { width: 100%; min-width: 940px; border-collapse: collapse; font-size: .84rem; }
  .operations-table th, .operations-table td { border-bottom: 1px solid #ece8e1; padding: .82rem .78rem; text-align: left; vertical-align: top; }
  .operations-table th { color: #64727c; background: #f8f6f1; font-size: .7rem; letter-spacing: .07em; text-transform: uppercase; }
  .operations-table tbody tr:last-child td { border-bottom: 0; }
  .operations-table td { color: #475765; line-height: 1.4; }
  .operations-table td strong { display: block; color: #1b2c3a; font-weight: 760; }
  .operations-table-detail { display: block; margin-top: .15rem; color: #75818a; font-size: .76rem; }
  .operations-table-status { color: #176444; font-weight: 730; text-transform: capitalize; }
  .operations-lead-disclosure { min-width: 7rem; }
  .operations-lead-disclosure summary { color: #294f68; cursor: pointer; font-size: .78rem; font-weight: 780; white-space: nowrap; }
  .operations-lead-disclosure[open] summary { margin-bottom: .75rem; }
  .operations-lead-details { display: grid; width: min(38rem, 62vw); grid-template-columns: repeat(2, minmax(0, 1fr)); gap: .7rem 1rem; margin: 0; border-radius: .55rem; padding: .8rem; background: #f7f5ef; }
  .operations-lead-details div { min-width: 0; }
  .operations-lead-details dt { color: #7b6d5a; font-size: .66rem; font-weight: 820; letter-spacing: .04em; text-transform: uppercase; }
  .operations-lead-details dd { margin: .18rem 0 0; color: #263846; font-size: .78rem; line-height: 1.45; overflow-wrap: anywhere; white-space: pre-wrap; }
  .operations-lead-detail-wide { grid-column: 1 / -1; }
  .operations-empty-cell { padding: 1.5rem !important; color: #77838d !important; text-align: center; }
  .operations-table-footer { display: flex; align-items: center; justify-content: space-between; gap: 1rem; margin-top: .9rem; }
  .operations-table-footer p { margin: 0; color: #75818a; font-size: .8rem; }
  .operations-pager { display: flex; gap: .45rem; }

  .operations-inbox-grid { grid-template-columns: minmax(0, 1.25fr) minmax(19rem, .75fr); align-items: start; }
  .operations-message-list { display: grid; gap: .65rem; margin: 1.25rem 0 0; padding: 0; list-style: none; }
  .operations-message { display: grid; gap: .25rem; border: 1px solid #e7e3dc; border-radius: .65rem; padding: .9rem; background: #fff; }
  .operations-message strong { color: #1b2c3a; font-size: .9rem; }
  .operations-message span { color: #6a7781; font-size: .78rem; line-height: 1.4; }
  .operations-message-meta { color: #75818a !important; font-size: .73rem !important; }
  .operations-message.is-clickable { cursor: pointer; }
  .operations-message.is-clickable:hover { border-color: #9ca9ae; background: #fbfaf7; }
  .operations-message.is-clickable:focus-visible { outline: 3px solid rgba(67, 112, 144, .32); outline-offset: 2px; }
  .operations-message-reader { margin-top: 1rem; border: 1px solid #d9e3df; border-radius: .7rem; padding: 1rem; background: #f8fcfa; }
  .operations-message-reader[hidden] { display: none; }
  .operations-message-reader-meta { margin: .8rem 0 0; color: #687782; font-size: .79rem; line-height: 1.5; }
  .operations-message-body { max-height: 25rem; overflow: auto; margin: 1rem 0 0; border: 1px solid #e5e2db; border-radius: .55rem; padding: .9rem; color: #354653; background: #fff; font-family: inherit; font-size: .86rem; line-height: 1.58; white-space: pre-wrap; word-break: break-word; }
  .operations-draft-form { display: grid; gap: .85rem; margin-top: 1.2rem; }
  .operations-draft-actions { display: flex; align-items: center; gap: .7rem; }
  .operations-draft-status { color: #63717b; font-size: .77rem; line-height: 1.4; }
  .operations-no-send-note { margin: 1.15rem 0 0; border-left: 3px solid #b38a4b; padding: .2rem 0 .2rem .7rem; color: #655944; font-size: .78rem; line-height: 1.52; }
  .operations-no-send-note strong { color: #59492e; }
  .operations-setup-list { display: grid; gap: .8rem; margin: 1.15rem 0 0; padding: 0; list-style: none; }
  .operations-setup-list li { position: relative; padding-left: 1.25rem; color: #5b6872; font-size: .86rem; line-height: 1.5; }
  .operations-setup-list li::before { position: absolute; left: 0; color: #a98245; content: "•"; font-weight: 900; }

  :root[data-theme="dark"] body,
  :root[data-theme="dark"] .operations-content { background: var(--operations-canvas); }
  :root[data-theme="dark"] .operations-sidebar { background: var(--operations-sidebar); }
  :root[data-theme="dark"] .operations-nav-link:hover { background: var(--operations-surface); }
  :root[data-theme="dark"] .operations-nav-link.is-active { color: var(--operations-text); background: var(--operations-surface); }
  :root[data-theme="dark"] .operations-nav-group-label { color: #777; }
  :root[data-theme="dark"] .operations-sidebar-note { border-color: var(--operations-border-soft); color: var(--operations-subtle); }
  :root[data-theme="dark"] .operations-theme-toggle { border-color: var(--operations-border); color: var(--operations-text); background: var(--operations-surface); }
  :root[data-theme="dark"] .operations-theme-toggle:hover { border-color: #5a5a5a; background: var(--operations-surface-hover); }

  :root[data-theme="dark"] .operations-main :where(h1, h2, h3, h4, strong, b, dt),
  :root[data-theme="dark"] :where(.operations-title, .operations-section-title, .operations-metric-value, .operations-list li strong, .operations-table td strong, .operations-message strong, .workspace-metric-value, .workspace-module-title, .workspace-pipeline-row strong, .workspace-followup-main strong, .workspace-connector-heading h3, .workspace-connector-number, .dashboard-panel-head a, .dashboard-opportunities a, .dashboard-source-list strong, .dashboard-quick-links a, .opportunity-title > a, .analytics-list li strong) { color: var(--operations-text-strong); }
  :root[data-theme="dark"] .operations-main :where(.operations-eyebrow, .operations-copy, .operations-metric-label, .operations-metric-copy, .operations-panel-copy, .operations-list li, .operations-toolbar-copy, .operations-field, .operations-table td, .operations-table-detail, .operations-table-footer p, .operations-message span, .operations-message-reader-meta, .operations-draft-status, .operations-setup-list li, .workspace-loading, .workspace-metric-label, .workspace-metric-detail, .workspace-module-copy, .workspace-empty, .workspace-pipeline-row, .workspace-followup-main span, .workspace-followup-meta time, .workspace-table td, .workspace-pagination-note, .workspace-connector-copy, .workspace-next-step p, .workspace-source-note, .dashboard-metric span, .dashboard-metric small, .dashboard-panel-head p, .dashboard-pipeline small, .dashboard-pipeline > p, .dashboard-attention a, .google-summary span, .google-summary small, .dashboard-insight-head span, .dashboard-opportunities > li > strong, .dashboard-source-list li, .analytics-basis span, .analytics-section-head > span, .analytics-metrics span, .search-metrics span, .session-compare span, .saved-lead-metrics span, .analytics-metrics small, .search-metrics small, .analytics-panel-head p, .session-chart > div small, .today-strip span, .today-strip small, .today-strip p, .analytics-explanation, .analytics-filters label span, .opportunity-rank, .opportunity-body > p, .query-list small, .opportunity-metrics dt, .opportunity-metrics small, .analytics-footnote, .analytics-list li, .analytics-empty) { color: var(--operations-muted); }

  :root[data-theme="dark"] .operations-shell .operations-main :where(.operations-panel, .operations-metric, .operations-message, .dashboard-metric, .analytics-basis > div, .analytics-metrics article, .search-metrics article, .workspace-metric-card, .workspace-module) { border-color: var(--operations-border); background: var(--operations-surface); box-shadow: none; }
  :root[data-theme="dark"] .operations-shell .operations-main :where(.operations-action-card.is-leads, .operations-action-card.is-workspace, .operations-action-card.is-inbox, .operations-action-card.is-analytics, .operations-message-reader, .operations-message-body, .operations-lead-details, .workspace-view-bar, .workspace-surface, .workspace-empty, .workspace-connector-card, .query-list > span, .analytics-footnote) { border-color: var(--operations-border); background: var(--operations-surface-soft); box-shadow: none; }
  :root[data-theme="dark"] .operations-shell .operations-main :where(.operations-status, .operations-input, .analytics-filters input, .analytics-filters select) { border-color: var(--operations-border); color: var(--operations-text); background: var(--operations-surface); }
  :root[data-theme="dark"] .operations-shell .operations-main :where(.operations-input, .analytics-filters input)::placeholder { color: var(--operations-subtle); }
  :root[data-theme="dark"] :where(.operations-link, .operations-primary-button) { border-color: #ececec; color: #171717; background: #ececec; }
  :root[data-theme="dark"] :where(.operations-link, .operations-primary-button):hover { border-color: #fff; color: #171717; background: #fff; }
  :root[data-theme="dark"] :where(.operations-link.is-quiet, .operations-button:not(.operations-primary-button)) { border-color: var(--operations-border); color: var(--operations-text); background: var(--operations-surface); }
  :root[data-theme="dark"] :where(.operations-link.is-quiet, .operations-button:not(.operations-primary-button)):hover:not(:disabled) { border-color: #5a5a5a; color: var(--operations-text); background: var(--operations-surface-hover); }

  :root[data-theme="dark"] .operations-main :where(.operations-table-wrap, .workspace-table-wrap) { border-color: var(--operations-border); }
  :root[data-theme="dark"] .operations-main :where(.operations-table th, .operations-table td, .workspace-table th, .workspace-table td, .dashboard-header, .dashboard-panel-head, .dashboard-attention li, .google-summary, .google-summary > div, .dashboard-opportunities li, .dashboard-quick-links, .dashboard-quick-links a, .analytics-header, .analytics-panel-head, .today-strip, .session-compare > div, .opportunity-card, .opportunity-metrics > div, .analytics-list li, .saved-lead-metrics > div, .workspace-followup, .workspace-next-step) { border-color: var(--operations-border); }
  :root[data-theme="dark"] .operations-main :where(.operations-table th, .workspace-table th) { color: var(--operations-muted); background: var(--operations-surface-soft); }
  :root[data-theme="dark"] .operations-main .workspace-table tbody tr:hover td { background: var(--operations-surface-hover); }
  :root[data-theme="dark"] .operations-main .operations-message.is-clickable:hover { border-color: #5a5a5a; background: var(--operations-surface-hover); }
  :root[data-theme="dark"] .operations-main .operations-message-body { color: var(--operations-text); }
  :root[data-theme="dark"] .operations-main .operations-lead-details dt { color: var(--operations-subtle); }
  :root[data-theme="dark"] .operations-main .operations-lead-details dd { color: var(--operations-text); }
  :root[data-theme="dark"] .operations-main .operations-lead-disclosure summary { color: #7fc9ff; }

  :root[data-theme="dark"] .operations-main :where(.operations-privacy-note, .workspace-source-note) { color: var(--operations-muted); background: var(--operations-info-soft); }
  :root[data-theme="dark"] .operations-main :where(.operations-status.is-ready, .operations-pill, .workspace-status-pill.is-ready, .dashboard-attention li.is-clear strong, .opportunity-badge.is-growing_visibility) { border-color: var(--operations-accent); color: #6ee7bd; background: var(--operations-accent-soft); }
  :root[data-theme="dark"] .operations-main :where(.operations-status.is-warning, .operations-pill.is-warning, .workspace-status-pill.is-warning, .opportunity-badge.is-near_page_one) { border-color: var(--operations-warning); color: var(--operations-warning); background: var(--operations-warning-soft); }
  :root[data-theme="dark"] .operations-main :where(.workspace-status-pill.is-error, .dashboard-attention li strong, .opportunity-badge.is-snippet_gap, .opportunity-badge.is-low_ctr) { color: var(--operations-danger); background: var(--operations-danger-soft); }
  :root[data-theme="dark"] .operations-main .operations-pill.is-muted,
  :root[data-theme="dark"] .operations-main .workspace-status-pill { color: var(--operations-muted); background: var(--operations-border-soft); }
  :root[data-theme="dark"] .operations-main .operations-no-send-note { color: var(--operations-warning); }
  :root[data-theme="dark"] .operations-main .operations-no-send-note strong { color: #ffd28a; }

  :root[data-theme="dark"] .operations-main .dashboard-metric.is-featured,
  :root[data-theme="dark"] .operations-main .analytics-metrics .is-featured,
  :root[data-theme="dark"] .operations-main .operations-metric.is-dark { border-color: #5a5a5a; color: var(--operations-text); background: #171717; }
  :root[data-theme="dark"] .operations-main :where(.dashboard-metric.is-featured span, .dashboard-metric.is-featured small, .analytics-metrics .is-featured span, .analytics-metrics .is-featured small, .operations-metric.is-dark .operations-metric-label, .operations-metric.is-dark .operations-metric-copy) { color: var(--operations-muted); }
  :root[data-theme="dark"] .operations-main :where(.dashboard-metric.is-featured strong, .analytics-metrics .is-featured strong, .operations-metric.is-dark .operations-metric-value) { color: var(--operations-text-strong); }
  :root[data-theme="dark"] .operations-main .analytics-basis { border-color: var(--operations-border); background: var(--operations-border); }
  :root[data-theme="dark"] .operations-main .analytics-filters select { accent-color: var(--operations-accent); }
  :root[data-theme="dark"] .operations-main .query-list > span { border-color: var(--operations-border); }
  :root[data-theme="dark"] .operations-main .opportunity-badge { color: var(--operations-muted); background: var(--operations-border-soft); }
  :root[data-theme="dark"] .operations-main .dashboard-pipeline div div span { background: #5b5b5b; }
  :root[data-theme="dark"] .operations-main .dashboard-pipeline > div:nth-child(2n) div span { background: #717171; }
  :root[data-theme="dark"] .operations-main .dashboard-pipeline > div:nth-child(3n) div span,
  :root[data-theme="dark"] .operations-main :where(.dashboard-source-list i, .session-chart > div span, .workspace-pipeline-bar) { background: var(--operations-accent); }
  :root[data-theme="dark"] .operations-main :where(.dashboard-source-list li > span:nth-child(2), .workspace-pipeline-track) { background: var(--operations-border-soft); }
  :root[data-theme="dark"] .operations-main .dashboard-opportunities span,
  :root[data-theme="dark"] .operations-main .workspace-next-step span { color: var(--operations-warning); }
  :root[data-theme="dark"] .operations-main .workspace-view-button { color: var(--operations-muted); }
  :root[data-theme="dark"] .operations-main .workspace-view-button:hover { color: var(--operations-text); background: var(--operations-surface-hover); }
  :root[data-theme="dark"] .operations-main .workspace-view-button.is-active { color: #171717; background: #ececec; box-shadow: none; }
  :root[data-theme="dark"] .operations-main .workspace-empty-mark { color: var(--operations-muted); background: var(--operations-border-soft); }
  :root[data-theme="dark"] .operations-main .workspace-error { color: var(--operations-muted); }
  :root[data-theme="dark"] .operations-main .workspace-error strong { color: var(--operations-danger); }
  :root[data-theme="dark"] .operations-main .workspace-error .workspace-error-help { color: var(--operations-subtle); }

  :root[data-theme="dark"] .operations-shell .operations-main :where(.dashboard-metric span, .dashboard-metric small, .dashboard-panel-head p, .dashboard-pipeline small, .dashboard-pipeline > p, .dashboard-attention a, .google-summary span, .google-summary small, .dashboard-insight-head span, .dashboard-opportunities > li > strong, .dashboard-source-list li, .analytics-basis span, .analytics-section-head > span, .analytics-metrics span, .search-metrics span, .session-compare span, .saved-lead-metrics span, .analytics-metrics small, .search-metrics small, .analytics-panel-head p, .session-chart > div small, .today-strip span, .today-strip small, .today-strip p, .analytics-explanation, .analytics-filters label span, .opportunity-rank, .opportunity-body > p, .query-list small, .opportunity-metrics dt, .opportunity-metrics small, .analytics-footnote, .analytics-list li, .analytics-empty, .workspace-loading, .workspace-metric-label, .workspace-metric-detail, .workspace-module-copy, .workspace-empty, .workspace-pipeline-row, .workspace-followup-main span, .workspace-followup-meta time, .workspace-table td, .workspace-pagination-note, .workspace-connector-copy, .workspace-next-step p, .workspace-source-note) { color: var(--operations-muted); }
  :root[data-theme="dark"] .operations-shell .operations-main :where(.dashboard-panel-head a, .dashboard-opportunities a, .dashboard-source-list strong, .dashboard-quick-links a, .opportunity-title > a, .analytics-list li strong, .workspace-metric-value, .workspace-module-title, .workspace-pipeline-row strong, .workspace-followup-main strong, .workspace-connector-heading h3, .workspace-connector-number) { color: var(--operations-text-strong); }

  @media (max-width: 980px) {
    .operations-shell { grid-template-columns: 1fr; }
    .operations-sidebar { display: none; }
    .operations-mobile-header { display: flex; align-items: center; justify-content: space-between; gap: 1rem; padding: 1.2rem 1.3rem .95rem; border-bottom: 1px solid #e1ded7; background: #fffdf9; }
    .operations-mobile-header .operations-brand { color: #131619; }
    .operations-mobile-header .operations-brand small { color: #697076; }
    .operations-mobile-secure { display: inline-flex; align-items: center; gap: .45rem; color: #536270; font-size: .76rem; font-weight: 730; }
    .operations-mobile-secure .operations-secure-dot { margin: 0; }
    .operations-mobile-nav { display: flex; overflow-x: auto; gap: .35rem; padding: .7rem 1.25rem; border-bottom: 1px solid #dedbd4; background: #fffdf9; }
    .operations-mobile-nav-link { flex: 0 0 auto; border-radius: 999px; padding: .52rem .75rem; color: #596874; font-size: .8rem; font-weight: 760; text-decoration: none; }
    .operations-mobile-nav-link.is-active { color: #fff; background: #172e40; }
    .operations-theme-toggle.is-compact { width: auto; min-height: 2.15rem; padding: .42rem .58rem; }
    :root[data-theme="dark"] .operations-mobile-header,
    :root[data-theme="dark"] .operations-mobile-nav { border-color: var(--operations-border); background: var(--operations-sidebar); }
    :root[data-theme="dark"] .operations-mobile-header .operations-brand { color: var(--operations-text); }
    :root[data-theme="dark"] .operations-mobile-header .operations-brand small,
    :root[data-theme="dark"] .operations-mobile-secure,
    :root[data-theme="dark"] .operations-mobile-nav-link { color: var(--operations-muted); }
    :root[data-theme="dark"] .operations-mobile-nav-link.is-active { color: #171717; background: #ececec; }
    .operations-main { width: min(1240px, calc(100% - 2.5rem)); padding-top: 2.3rem; }
    .operations-inbox-grid { grid-template-columns: 1fr; }
  }

  @media (max-width: 760px) {
    .operations-page-header { align-items: flex-start; flex-direction: column; gap: 1rem; }
    .operations-status { max-width: none; }
    .operations-metrics, .operations-action-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .operations-filters { grid-template-columns: 1fr 1fr; }
    .operations-filters .operations-field:nth-child(2), .operations-filters .operations-button { grid-column: 1 / -1; }
  }

  @media (max-width: 540px) {
    .operations-main { width: min(100% - 1.25rem, 1240px); padding-top: 1.7rem; padding-bottom: 3rem; }
    .operations-mobile-header { padding-right: .75rem; padding-left: .75rem; }
    .operations-mobile-nav { padding-right: .7rem; padding-left: .7rem; }
    .operations-mobile-secure { display: none; }
    .operations-theme-toggle.is-compact [data-theme-label] { position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; }
    .operations-title { font-size: clamp(2.45rem, 13vw, 3.5rem); }
    .operations-panel { padding: 1.1rem; }
    .operations-metrics, .operations-action-grid, .operations-breakdown-grid { grid-template-columns: 1fr; }
    .operations-analytics-metrics { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .operations-metric { min-height: auto; }
    .operations-filters { grid-template-columns: 1fr; }
    .operations-filters .operations-field:nth-child(2), .operations-filters .operations-button { grid-column: auto; }
    .operations-table-footer, .operations-toolbar { align-items: flex-start; flex-direction: column; }
    .operations-lead-details { width: min(36rem, 78vw); grid-template-columns: 1fr; }
    .operations-lead-detail-wide { grid-column: auto; }
  }
</style>
